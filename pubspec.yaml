name: twidgets
description: "A Flutter library with useful widgets and utilities."
version: 0.0.1
homepage: https://github.com/vitor-hugo/twidgets
topics: ["container", "shadow", "clipped", "widgets", "library"]
platforms:
  android:
  ios:
  macos:
  linux:
  windows:

environment:
  sdk: ^3.13.1
  flutter: ">=3.0.0"

workspace:
  - example

dependencies:
  flutter:
    sdk: flutter

  material_ui: ^1.1.0
  cupertino_ui: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
